1.おそらく、GUIデータが存在しないとでかくなる可能性がある
modelObjectにGameObjectのスケールが代入されている？

2.GUIDが存在しているかつ存在してるデータの番号を超過するとでかくなる
例　Player_2.guidまで存在している
読み込みを繰り返し、Player_3が誕生するとでかくなる

3.PlayerのコンストラクタのSetScale(3)でmodelObjectもでかくなってる

4.モデルオブジェクトだけが書き変わっていて、GameObjectが書き変わっていない状態でおかしくなる

5.GameObjectのSet関数を使って動かすとき、座標はモデルオブジェクトも動かしてよいが
スケールはダメ？　アングルは動かしていい

6.そもそもSetScaleを呼び出してるのに大きくなってない最初が異常では？

7.GUIでスケールが変わらなくなってる(反映されてない？)ので、それを直せば自然解決すると思われる

8.RegisterObjectでSetScaleをしたら絶対変更前1、変更後3になってモデルがでかくなるのになってない
GUIDから読み取ったデータで上書きされている可能性が高い？

9.コンストラクタにSetScaleなどの処理を書いてたのが悪かった
これを徹底したほうがいいと思われる